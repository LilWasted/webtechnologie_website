extends layout

block content
    h1= title
    if user
        h2 Username: #{user.username}
        h3 Email: #{user.email}
        h3 Rating: #{user.rating}
        h3 Events:
        ul
            each event in user.events
                li
                    a(href=event.url) #{event.title}
    else
        p User not found.